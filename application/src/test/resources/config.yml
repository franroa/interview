server:
  applicationConnectors:
    - type: http
      port: 8181
  adminConnectors:
    - type: http
      port: 9002

database:
  driverClass: org.postgresql.Driver
  password: password
  user: postgres
  # url: jdbc:postgresql://localhost:54322/interviewDB-test TODO -> to be abstracted (is hard coded in TestCase) I want to create two config files for testing
  url: fake TODO -> to be abstracted

queue:
  type: "SYNC"
